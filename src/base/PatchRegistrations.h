// patches update this function to register self; included in PatchRegistry.cc
static
bool RegisterPatches() {
	/* add patch registrations here */


	// keep the two empty lines above to enable fuzz-free patching with -U4 patches
	return true;
}
